What is the difference between position: absolute and position: fixed ? 
    "Position: absolute" positions the element relative to a parent element. 
    Fixed makes the element static, staying still even if the user scrolls the page.
With CSS3, how do we access the third element within another element?
    Using the nth-child selector. For example, if you want to select the third paragraph you'd write p:nth-child(3){}